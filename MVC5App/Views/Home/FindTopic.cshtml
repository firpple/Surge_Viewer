


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>CompanyQueryByTopic</title>
</head>
<body>
    <div>
    </div>
</body>
</html>
<form method="POST">
    <div>Topic name contains: <input name="TopicName" /></div>
    <input type="submit" />
</form>
@{
    string queryString = "";
    string[] topicList;
    if (IsPost)
    {
        var topicString = Request.Form["TopicName"];
        var tempQueryTools = new MVC5App.Scripts.Test.QueryTools();
        queryString = tempQueryTools.findTopicWithString(topicString, "%");
        topicList = queryString.Split('%');

        <body>
            Topics that contain @topicString
        </body>
        <table style="width:100%">
            @foreach (string topic in topicList)
            {
                <tr><th> @topic</th></tr>
            }
        </table>

    }

}



@{
    Html.BeginForm("HandleForm", "Home", FormMethod.Get);
    <p>Hi</p>  
    <input type="text" name="companyName" value="Company" />
    <input type="text" name="topicName" value="Topic" />
    <input type="submit" value="Submit"/>
    Html.EndForm();

}